할 것

1. 네트워크 통신이 이루어 지므로 로그함수 만들어서 모든 과정을 log 파일에 저장해야 함
2. keylogger.c 에서 echo_server 를 실행 할 수 있도록 init , exit 부분 수정 필요
3. klg_load.sh 와 klg_unload.sh 수정 필요 (인자 값을 받아서 프로그램 내부에서 처리할 수 있도록 변경)

끝난 것
1. 특정 클라이언트만 access하도록 수정
2. 프로그램 실행시 인자 값 처리
